class Filter {
  final int id;
  final String title;
  final String description;
  bool isChecked;

  Filter({
    required this.id,
    required this.isChecked,
    required this.title,
    required this.description,
  });
}
